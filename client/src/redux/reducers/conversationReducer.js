const initialState = {
  userMessaging: false,
  user: {},
  conversationId: null,
  messages: []
};

const messageReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    
  }
}